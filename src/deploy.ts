/**
 * Notice: Server-side funded deployment has been removed.
 *
 * Contract deployment is performed directly in the browser via the 1AM wallet extension
 * on Midnight Preprod at the `/deploy` route.
 *
 * Run `npm run frontend:dev` and navigate to `/deploy` in your browser.
 */

console.log("=========================================================================");
console.log("  Server-side deployer wallet script has been removed.");
console.log("  Contract deployment is handled in the browser via 1AM extension.");
console.log("  Please run: npm run frontend:dev and open http://localhost:5173/deploy");
console.log("=========================================================================");
process.exit(0);
